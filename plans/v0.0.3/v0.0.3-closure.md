# v0.0.3 - Closure

> **Parent:** `plans/v0.0.3/00-program.md` - program ledger
> **Status:** not started. Per-phase closure rows live in each phase file.
> **Estimated effort:** done with the plan

---

## Overview

Each code-changing phase records its own `make lint` and `make test` rows. This file records the merged tree once the phases are integrated, the same shape as `plans/v0.0.2/v0.0.2-closure.md`.

Local `make test` is the gate. This file does not add CI.

## Phase 1: Merged tree

### 1.1 Lint

- [ ] `make lint` passes on the integration branch with the landed phases merged. Outcome on YYYY-MM-DD: exit 0. `gofmt -l .` printed nothing. `golangci-lint run ./...` exited 0. `size-check` reported 0 over-limit files.

### 1.2 Test

- [ ] `make test` passes on the same tree. Outcome on YYYY-MM-DD: exit 0. The command was `go test -p <n> ./...`.

## Dependencies

None. The closure waits for the phases it records, and it does not depend on any single one.
