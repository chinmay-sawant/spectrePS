# v0.0.3 - Future work

> **Parent:** `plans/v0.0.1/10-deferred.md` - deferred rows that outgrew one line
> **Status:** proposed. No row is active. The deferred row in `plans/v0.0.1/10-deferred.md` points here.
> **Estimated effort:** weeks, behind the text and image work

---

## Overview

This folder holds future work that was deferred with a detail file. Nothing here is scheduled. A file becomes active only when a phase is checked in this folder the same way `plans/v0.0.2/` was checked.

## Phase index

| File | Order | Job |
| --- | --- | --- |
| `1-pdf-compression.md` | proposed | Compression levels 1 to 5 over any PDF the reader can open. |

## Dependencies

The compression plan depends on an object pass-through writer, the image model, and text or pass-through for content operators Spectre does not interpret.
