# v0.0.2 - Closure

> **Parent:** `plans/v0.0.2/00-program.md` - release ledger
> **Status:** checked. Phases 1 to 5 are checked in their own files. Merged-tree lint and test recorded in row 4.4.
> **Estimated effort:** done with the plan

---

## Overview

`go test -p n` is how many test binaries run at once. The default is `GOMAXPROCS`. `-parallel` is different: it only limits `t.Parallel` tests inside one package. Source: [cmd/go, -p](https://pkg.go.dev/cmd/go).

Local `make test` is the gate. This file does not add CI.

## Executive summary

`make test` now passes `-p` with the machine's CPU count, from `nproc`, so package tests run together instead of leaving the parallelism implicit. `make lint` is unchanged. Phases 1 to 5 are closed in their own files.

## Phase 4: Test gate

### 4.1 Parallel test binaries

- [x] `Makefile` `test` runs `go test -p $(NPROC) ./...`. `NPROC` is `nproc`, or 1 when `nproc` is missing. `make help` and `documentation/development.md` name that command. Proof: `grep -n 'go test -p' Makefile documentation/development.md` on 2026-09-25 shows both lines.

### 4.2 Lint

- [x] `make lint` passes. Outcome on 2026-09-25: exit 0. `gofmt -l .` printed nothing. `golangci-lint run ./...` exited 0. `size-check` reported 0 over-limit files.

### 4.3 Local tests

- [x] `make test` passes. Outcome on 2026-09-25: exit 0. The command was `go test -p 24 ./...`. Local `go test` is enough. No CI change.

```
?   	github.com/chinmay-sawant/spectrePS/cmd/spectreps	[no test files]
ok  	github.com/chinmay-sawant/spectrePS/internal/cli	0.061s
?   	github.com/chinmay-sawant/spectrePS/internal/engine	[no test files]
ok  	github.com/chinmay-sawant/spectrePS/internal/graphics	0.005s
ok  	github.com/chinmay-sawant/spectrePS/internal/pdf	0.014s
ok  	github.com/chinmay-sawant/spectrePS/internal/pdfout	0.016s
ok  	github.com/chinmay-sawant/spectrePS/internal/ps	0.018s
ok  	github.com/chinmay-sawant/spectrePS/spectreps	0.030s
```

### 4.4 Merged tree

- [x] `make lint` passes on `feature/002-quick-wins-impl` with phases 1 to 3 merged. Outcome on 2026-09-25: exit 0. `gofmt -l .` printed nothing. `golangci-lint run ./...` exited 0. `size-check` reported 0 over-limit files.
- [x] `make test` passes on the same tree. Outcome on 2026-09-25: exit 0. The command was `go test -p 24 ./...`. All packages were ok; `cmd/spectreps` and `internal/engine` have no test files.

## Dependencies

None. This phase does not wait for the bbox, JPEG, or bitmap PDF rows.
