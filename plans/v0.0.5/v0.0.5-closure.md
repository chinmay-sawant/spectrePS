# v0.0.5 - Closure

> **Parent:** `plans/v0.0.5/00-program.md` - program ledger
> **Status:** planned
> **Estimated effort:** one run

---

## Overview

The gate for the merged tree. Both commands run on the integration branch with every phase of `plans/v0.0.5/` merged, and the outcome of each is written into this file rather than left in a chat transcript.

The four rows below record the development-tree run on 2026-09-26, on `master` with the v0.0.5 changes in the working tree and not committed. That is not the merged-tree run, and `documentation/development.md` says a green development loop is not a substitute for the tag line. The integrator re-runs rows 1.1 to 1.4 after the merge and replaces these outcomes.

## Phase 1: Development tree

- [x] 1.1 `make lint` on the development tree. Outcome on 2026-09-26: exit 0. `gofmt -l .` printed nothing. `golangci-lint run ./...` exited 0 after five findings in the new benchmark code, which were a named return in `internal/engine/bench_test.go`, an unused boolean in `internal/font/bench_test.go`, a short variable name in `internal/graphics/bench_test.go`, a named return in `internal/pdf/bench_test.go`, and an `fmt.Sprintf` that `strconv.Itoa` replaces in `internal/tag/bench_test.go`. `size-check` reported 0 over-limit files, and the largest new file is `spectreps/bench_test.go` at 401 lines.
- [x] 1.2 `make test NPROC=4` on the same tree. Outcome on 2026-09-26: exit 0. The command was `go test -p 4 ./...`; all 13 packages reported ok, `internal/cli` in 5.399s, `spectreps` in 1.437s, `internal/pdfout` in 1.092s, `internal/pdf` in 0.848s, and `internal/validation` in 0.043s. The new `TestJobAllocs` and `TestEncoderAllocs` ran inside that and passed at the recorded ceilings.
- [x] 1.3 `make build` on the same tree. Outcome on 2026-09-26: exit 0. `go build -trimpath -o bin/spectreps ./cmd/spectreps` produced a 7577021 byte binary and `bin/spectreps version` printed `0.0.4`. The version is unchanged because v0.0.5 is not released and this file adds no product behaviour.
- [x] 1.4 `make bench` on the same tree. Outcome on 2026-09-26: exit 0 after 6 minutes 7 seconds. `profiles/bench.txt` holds 324 lines and 258 benchmark rows across the eleven packages. `make bench-check` also ran and produced the first real benchstat verdict on this tree, which `documentation/performance.md` section `bench-check` records, including why that verdict is not yet usable and the deferred row 4.3 that fixes it.

## Phase 2: Merged tree

- [ ] 2.1 `make lint` on the integration branch with the landed phases merged, recorded in this row. Proof: `make lint`.
- [ ] 2.2 `make test` on the same tree, recorded in this row. Proof: `make test`.
- [ ] 2.3 `make build` on the same tree, recorded in this row. Proof: `make build`.
- [ ] 2.4 `make bench` on the same tree, recorded in this row, so the release transcript carries the eleven-package suite. Proof: `make bench`.

## Dependencies

The integrator runs phase 2. Every row needs the merged tree, so none of them closes before the phases in `1-graphics-device.md` through `5-baseline-and-budget.md` land. Phase 2 does not close while deferred row 4.3 of `5-baseline-and-budget.md` is open, because that row decides whether the recorded timing numbers are a baseline or a first reading.
