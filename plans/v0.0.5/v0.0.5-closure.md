# v0.0.5 - Closure

> **Parent:** `plans/v0.0.5/00-program.md` - program ledger
> **Status:** phases 1 and 2 partially closed. The development tree passed all four rows on 2026-09-26. The merged tree `76a95d9` passed `make lint`, `make test`, and `make build`. Row 2.4 is unrun and deferred row 4.3 of `5-baseline-and-budget.md` is open, so no timing number in the tag is a baseline.
> **Estimated effort:** one run for row 2.4, plus the decision row 4.3 asks for

---

## Overview

The gate for the merged tree. Both commands run on the integration branch with every phase of `plans/v0.0.5/` merged, and the outcome of each is written into this file rather than left in a chat transcript.

Phase 1 records the development-tree run on 2026-09-26, on `master` with the v0.0.5 changes in the working tree and not committed. Phase 2 records the merged-tree run at `76a95d9`, after pull request #16 merged. `documentation/development.md` says a green development loop is not a substitute for the merged-tree run, which is why both phases are here and why phase 1 is kept rather than overwritten.

Two rows remain open. Row 2.4 wants a `make bench` on the merged tree, which is six minutes. Deferred row 4.3 of `5-baseline-and-budget.md` wants a `-count=10` re-capture before any timing number is treated as a baseline, and that row needs a decision from the integrator rather than more work from this file.

## Phase 1: Development tree

- [x] 1.1 `make lint` on the development tree. Outcome on 2026-09-26: exit 0. `gofmt -l .` printed nothing. `golangci-lint run ./...` exited 0 after five findings in the new benchmark code, which were a named return in `internal/engine/bench_test.go`, an unused boolean in `internal/font/bench_test.go`, a short variable name in `internal/graphics/bench_test.go`, a named return in `internal/pdf/bench_test.go`, and an `fmt.Sprintf` that `strconv.Itoa` replaces in `internal/tag/bench_test.go`. `size-check` reported 0 over-limit files, and the largest new file is `spectreps/bench_test.go` at 401 lines.
- [x] 1.2 `make test NPROC=4` on the same tree. Outcome on 2026-09-26: exit 0. The command was `go test -p 4 ./...`; all 13 packages reported ok, `internal/cli` in 5.399s, `spectreps` in 1.437s, `internal/pdfout` in 1.092s, `internal/pdf` in 0.848s, and `internal/validation` in 0.043s. The new `TestJobAllocs` and `TestEncoderAllocs` ran inside that and passed at the recorded ceilings.
- [x] 1.3 `make build` on the same tree. Outcome on 2026-09-26: exit 0. `go build -trimpath -o bin/spectreps ./cmd/spectreps` produced a 7577021 byte binary and `bin/spectreps version` printed `0.0.4`. The version is unchanged because v0.0.5 is not released and this file adds no product behaviour.
- [x] 1.4 `make bench` on the same tree. Outcome on 2026-09-26: exit 0 after 6 minutes 7 seconds. `profiles/bench.txt` holds 324 lines and 258 benchmark rows across the eleven packages. `make bench-check` also ran and produced the first real benchstat verdict on this tree, which `documentation/performance.md` section `bench-check` records, including why that verdict is not yet usable and the deferred row 4.3 that fixes it.

## Phase 2: Merged tree

Tree under test: `76a95d9`, the merge commit for pull request #16, with a clean working tree and go1.26.4 on linux/amd64. Run on 2026-09-26.

- [x] 2.1 `make lint` on the integration branch with the landed phases merged, recorded in this row. Proof: `make lint`. Outcome on 2026-09-26 at `76a95d9`: exit 0. `gofmt -l .` printed nothing, `golangci-lint run ./...` exited 0 with no findings, and `size-check` reported 0 over-limit files.
- [x] 2.2 `make test` on the same tree, recorded in this row. Proof: `make test`. Outcome on 2026-09-26 at `76a95d9`: exit 0. The first run returned nine packages as `(cached)`, which is weak evidence for a gate, so the test cache was cleared with `go clean -testcache` and the run repeated as `go test -p 4 -count=1 ./...`. All 12 packages with tests reported ok: `internal/cli` 6.448s, `internal/ps` 2.950s, `spectreps` 2.778s, `internal/pdfout` 0.936s, `internal/pdf` 0.609s, `internal/pdfa` 0.015s, `internal/validation` 0.013s, `internal/tag` 0.009s, `internal/font` 0.008s, `internal/psout` 0.005s, `internal/graphics` 0.004s, and `internal/engine` 0.003s with no tests to run. Three packages hold no test files. Wall clock 6.8 seconds. The three allocation tests were then run alone and verbose, and `TestPerformanceAllocs`, `TestJobAllocs`, and `TestEncoderAllocs` each reported PASS, so the ceilings are confirmed rather than inferred from the package result.
- [x] 2.3 `make build` on the same tree, recorded in this row. Proof: `make build`. Outcome on 2026-09-26 at `76a95d9`: exit 0. `bin/` was deleted first so the build was real rather than a no-op. `go build -trimpath -o bin/spectreps ./cmd/spectreps` produced a 7577005 byte binary and `bin/spectreps version` printed `0.0.4`. That is 16 bytes under the 7577021 byte binary from row 1.3 on a tree where no production file changed, so the delta is build-path nondeterminism and not a code effect.
- [ ] 2.4 `make bench` on the same tree, recorded in this row, so the release transcript carries the twelve-package suite. Proof: `make bench`. Not run at `76a95d9`. The row 1.4 capture is on the development tree and this file does not treat it as the merged-tree equivalent.

## Dependencies

The integrator runs phase 2. Every row needs the merged tree, so none of them closes before the phases in `1-graphics-device.md` through `5-baseline-and-budget.md` land. Phase 2 does not close while deferred row 4.3 of `5-baseline-and-budget.md` is open, because that row decides whether the recorded timing numbers are a baseline or a first reading. Row 2.4 is the only row here that is work rather than a decision.
