package pdf

// pageBody is the shared one-page dictionary for synthetic test PDFs. It is
// package-level so the hand-built fixtures do not repeat the literal.
const pageBody = "<< /Type /Page /Parent 2 0 R /Contents 4 0 R >>"
