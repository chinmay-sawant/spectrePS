// Package font holds the metrics and encodings the text machine reads.
// The tables are generated and checked in, so the package stays offline and
// pure Go. Phase 2 adds outlines, ToUnicode, and Type0 mapping on top.
package font
