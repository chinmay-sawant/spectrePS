// Package font holds the metrics and encodings the text machine reads, plus
// the Type 1 program decoder. The tables are generated and checked in, so the
// package stays offline and pure Go.
package font
